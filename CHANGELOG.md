# Changelog

All notable changes to this project will be documented in this file.

## [Unreleased]
- Phase 2: Memory integration — recall included in prompt, memory attestations attached to audit traces.
- CI: Add multi-OS Python test matrix and linting.
- Docs: Add CONTRIBUTING and dev quickstart.
- Cleanup: Remove stale `cli.py.debug` artifact and add `*.debug` to `.gitignore`.
- Cleanup: Add missing `__init__.py` to `tests/gateway/`.
- Fix: Correct `.gitignore` path from `hermes-prime/bin/.keep` to `.hermes-prime/bin/.keep`.
- Lint: Apply `ruff format` across 91 source files for consistent code style.

## [0.2.0] - 2026-05-23
- Initial public beta release with governance-first architecture and Sentinel core.

## [0.2.1] - 2026-05-23
- Phase 2: Memory integration — recall included in prompt, memory attestations attached to audit traces.
- CI and docs improvements.
